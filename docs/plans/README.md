# 計劃與改進文檔

本目錄包含專案的分階段計劃和未來改進方案。

## 文檔說明

### 分階段計劃

**注意**：註解系統漸進式推廣策略已完成，實施結果已整合到 [Husky 整合](../annotations/husky-integration.md) 文檔中。

### 改進方案

- [維護系統未來改進](./maintenance-improvements.md) - ARCHITECTURE.md 自動維護系統的未來改進計劃，包含更精確的檢查、自動更新、CI/CD 整合等改進方向
- [TDD 語意分析檢閱與後續改進計劃](./tdd-semantic-review.md) - 完整的語意分析檢閱計劃，從 feature.md 到實作 .tsx/.ts，包含後續建議實作和語意一致性檢閱

**注意**：註解系統改進方案已完成，結果已整理到 `docs/annotations/` 目錄下的對應文檔中。

## 文檔分類規則

### Plan 類文件（`docs/plans/` 目錄）

以下類型的內容應放在 `docs/plans/` 目錄：

- [x] **分階段計劃** - 包含多個階段、里程碑的計劃
- [x] **未來改進** - 尚未實施的改進方案
- [x] **待完成功能** - 計劃中但尚未完成的功能
- [x] **長期目標** - 需要長期規劃的目標

### Plan 文件結構規則

#### 規則：衍生目標集中管理

**在 plan 實作中，若產生當下不會完成的項目描述**（如短中長期改善、建議項目、未來優化等），**一律集中整理成衍生目標的項目清單，放在 plan 的第二段（"目標" 段落之後）**。

#### 規則：優先執行項目管理

**在 plan 實作過程中，如果生成建議先執行的項目**（如修正測試失敗、修復關鍵 bug、驗證已完成功能等），**必須安排先列入到 plan md 中順序上優先執行之位置，並立即執行之**。

**處理流程**：

1. **識別優先項目**：在執行計劃過程中，如果發現需要優先處理的項目（如測試失敗、關鍵 bug、驗證需求等），應立即識別。
2. **調整計劃順序**：將優先項目移到計劃文件的優先位置（通常在"階段 1"之前，或作為"階段 0"或"優先執行"段落）。
3. **立即執行**：優先項目應立即執行，完成後再繼續原計劃的後續階段。

**結構範例**：

```markdown
# 計劃名稱

## 目標

[主要目標描述]

## 衍生目標

[當下不會完成的項目]

## 優先執行項目

以下項目需要優先處理，應在正式階段開始前完成：

- [ ] **優先項目 1** - [描述] - [原因]
- [ ] **優先項目 2** - [描述] - [原因]

## 階段 1: [階段名稱]

[實際要執行的階段內容]
```

**原則**：

- ✅ 優先項目應放在"衍生目標"之後，"階段 1"之前
- ✅ 優先項目應明確標註優先原因
- ✅ 優先項目應立即執行，不應延後
- ✅ 優先項目完成後，應更新計劃文件，標記為已完成
- ❌ 不要將優先項目混入一般執行階段中

**結構範例**：

```markdown
# 計劃名稱

## 目標

[主要目標描述]

## 衍生目標

以下項目為計劃執行過程中產生的改進建議和未來優化方向，不在當前計劃範圍內，但值得後續考慮：

### 短期改進

- [ ] 改進項目 1
- [ ] 改進項目 2

### 中期改進

- [ ] 改進項目 3
- [ ] 改進項目 4

### 長期優化

- [ ] 優化項目 1
- [ ] 優化項目 2

## 階段 1: [階段名稱]

[實際要執行的階段內容]
```

**原則**：

- ✅ 衍生目標應放在"目標"段落之後，"階段 1"之前
- ✅ 使用 checkbox 格式（`- [ ]`）以便追蹤
- ✅ 可以按優先級分類（短期、中期、長期）
- ✅ 衍生目標不應混入實際執行階段中
- ❌ 不要在執行階段中描述"建議"、"未來改進"等當下不會完成的項目

### 非 Plan 類文件（功能文檔）

以下類型的內容應放在對應的功能文檔中（如 `docs/annotations/*.md`、`docs/features/*.md`、`docs/maintenance/*.md` 等）：

- [x] **已實施功能** - 已經完成的功能說明
- [x] **當前狀態** - 系統當前的運作方式
- [x] **使用指南** - 如何使用現有功能

**重要原則**：非 plan 類文件必須描述**最終完成狀態**，而非階段規劃。當將 plan 的實施結果整合到非 plan 類文件時，必須遵循以下寫作原則：

#### 原則 1: 描述最終完成狀態，而非階段規劃

❌ **錯誤範例**（階段規劃描述）：

```markdown
### 階段配置

#### Phase 1: 試點階段（已完成 ✅）

- **狀態**：completed
- **目標組件**：Dashboard

#### Phase 2: 擴展階段（已完成 ✅）

- **狀態**：completed
- **目標組件**：Dashboard, WordLibrary, LearningMode

#### Phase 3: 全面推廣（進行中 🚀）

- **狀態**：active
- **目標組件**：所有組件（\*）
```

✅ **正確範例**（最終完成狀態描述）：

```markdown
### 當前配置狀態

系統已全面啟用註解檢查：

- **檢查範圍**：所有 `components/` 和 `services/` 目錄下的 `.tsx` 和 `.ts` 檔案
- **嚴格度**：error（所有變更都必須有註解，否則阻止提交）
- **格式檢查**：啟用格式錯誤檢查（START/END 不匹配會阻止提交）
```

#### 原則 2: 移除階段性描述，改為功能說明

❌ **錯誤範例**：

```markdown
## 漸進式推廣策略

系統採用三階段漸進式推廣策略：

1. Phase 1（試點階段）：在 Dashboard 組件中驗證系統
2. Phase 2（擴展階段）：擴展到主要組件
3. Phase 3（全面推廣）：推廣到所有組件
```

✅ **正確範例**：

```markdown
## 系統狀態

### 註解覆蓋統計

- **總組件數**：10 個
- **總註解標記數**：79 個

### 系統功能

- [x] Pre-commit hook 已配置並運作
- [x] 格式錯誤檢查已啟用
- [x] 所有組件變更都需要註解
```

#### 原則 3: 使用現在時態，描述當前運作方式

❌ **錯誤範例**：

```markdown
系統目前處於 Phase 3（全面推廣）階段：

- 目標組件：所有組件（\*）
- 嚴格度：error（阻止提交）
```

✅ **正確範例**：

```markdown
系統已全面啟用註解檢查：

- 檢查範圍：所有 `components/` 和 `services/` 目錄下的檔案
- 嚴格度：error（所有變更都必須有註解，否則阻止提交）
```

#### 原則 4: 保留配置結構，但描述當前狀態

配置範例中的階段結構（如 `phase3`）可以保留，因為這是實際配置文件的結構，但文檔描述應聚焦於當前運作狀態：

```json
{
  "phases": {
    "phase3": {
      "status": "active",
      "components": ["*"],
      "strictness": "error"
    }
  }
}
```

文檔中應描述為：

```markdown
### 當前配置狀態

系統已全面啟用註解檢查：

- **檢查範圍**：所有組件（`*`）
- **嚴格度**：error（阻止提交）
```

#### 檢查清單

在整合 plan 結果到非 plan 類文件時，確認：

- [ ] 已移除所有階段規劃描述（Phase 1、2、3 等）
- [ ] 已改為描述最終完成狀態
- [ ] 已移除「進行中」、「已完成」等階段性標記
- [ ] 已使用現在時態描述當前運作方式
- [ ] 已將「階段配置」改為「當前配置狀態」或「系統狀態」
- [ ] 已將「實施結果」改為「系統功能」或「功能說明」
- [ ] 已將「推廣策略」改為「系統運作方式」或「使用說明」

## Checkbox 追蹤規則

所有計劃文件中的步驟和行動項目都應使用 checkbox 格式（`- [ ]`）以便追蹤進度：

### 格式

```markdown
**行動**：

- [ ] 步驟 1
- [ ] 步驟 2
- [ ] 步驟 3
```

### 使用方式

- **未完成**：使用 `- [ ]`（空白 checkbox）
- **已完成**：使用 `- [x]`（勾選 checkbox）
- **進行中**：可以在項目後添加 `(進行中)` 標記

### 範例

```markdown
**行動**：

- [x] 在 Dashboard 組件中添加註解標記（已完成）
- [ ] 測試 pre-commit hook（進行中）
- [ ] 收集反饋並調整
```

這樣可以清楚地追蹤每個計劃項目的進度狀態。

## Plan 完成後的處理流程

### 完成標準

當 plan 中的所有項目都完成時（所有 checkbox 標記為 `- [x]`），需要進行以下處理：

### 步驟 1: 整理結果到對應文檔

**必須將 plan 中的各項變更結果更新到對應文件**：

1. **功能相關的 plan**：

   - 更新或創建 `docs/features/*.md` 文件
   - 更新 `docs/features/README.md`（功能列表）
   - 更新 `ARCHITECTURE.md`（如涉及架構變更）

2. **註解系統相關的 plan**：

   - 更新 `docs/annotations/*.md` 文件
   - 例如：`docs/annotations/system.md`、`docs/annotations/examples.md`

3. **維護系統相關的 plan**：

   - 更新 `docs/maintenance/*.md` 文件
   - 例如：`docs/maintenance/maintenance.md`

4. **工具或腳本相關的 plan**：
   - 更新對應的使用說明文檔
   - 更新 `package.json` 中的 scripts（如適用）

**原則**：

- [x] 將所有實施結果整理到對應的文檔目錄
- [x] 如果沒有對應的文檔，創建新的說明文件
- [x] 確保文檔完整且準確反映實施結果

### 非 Plan 類文件的寫作原則

**重要**：當將 plan 的實施結果整合到非 plan 類文件（如 `docs/annotations/*.md`、`docs/features/*.md` 等）時，必須遵循以下原則：

#### 原則 1: 描述最終完成狀態，而非階段規劃

❌ **錯誤範例**（階段規劃描述）：

```markdown
### 階段配置

#### Phase 1: 試點階段（已完成 ✅）

- **狀態**：completed
- **目標組件**：Dashboard
- **嚴格度**：warning

#### Phase 2: 擴展階段（已完成 ✅）

- **狀態**：completed
- **目標組件**：Dashboard, WordLibrary, LearningMode

#### Phase 3: 全面推廣（進行中 🚀）

- **狀態**：active
- **目標組件**：所有組件（\*）
```

✅ **正確範例**（最終完成狀態描述）：

```markdown
### 當前配置狀態

系統已全面啟用註解檢查：

- **檢查範圍**：所有 `components/` 和 `services/` 目錄下的 `.tsx` 和 `.ts` 檔案
- **嚴格度**：error（所有變更都必須有註解，否則阻止提交）
- **格式檢查**：啟用格式錯誤檢查（START/END 不匹配會阻止提交）
```

#### 原則 2: 移除階段性描述，改為功能說明

❌ **錯誤範例**：

```markdown
## 漸進式推廣策略

系統採用三階段漸進式推廣策略：

1. Phase 1（試點階段）：在 Dashboard 組件中驗證系統
2. Phase 2（擴展階段）：擴展到主要組件
3. Phase 3（全面推廣）：推廣到所有組件
```

✅ **正確範例**：

```markdown
## 系統狀態

### 註解覆蓋統計

- **總組件數**：10 個
- **總註解標記數**：79 個

### 系統功能

- [x] Pre-commit hook 已配置並運作
- [x] 格式錯誤檢查已啟用
- [x] 所有組件變更都需要註解
```

#### 原則 3: 使用現在時態，描述當前運作方式

❌ **錯誤範例**：

```markdown
系統目前處於 Phase 3（全面推廣）階段：

- 目標組件：所有組件（\*）
- 嚴格度：error（阻止提交）
```

✅ **正確範例**：

```markdown
系統已全面啟用註解檢查：

- 檢查範圍：所有 `components/` 和 `services/` 目錄下的檔案
- 嚴格度：error（所有變更都必須有註解，否則阻止提交）
```

#### 原則 4: 保留配置結構，但描述當前狀態

配置範例中的階段結構（如 `phase3`）可以保留，因為這是實際配置文件的結構，但文檔描述應聚焦於當前運作狀態：

```json
{
  "phases": {
    "phase3": {
      "status": "active",
      "components": ["*"],
      "strictness": "error"
    }
  }
}
```

文檔中應描述為：

```markdown
### 當前配置狀態

系統已全面啟用註解檢查：

- **檢查範圍**：所有組件（`*`）
- **嚴格度**：error（阻止提交）
```

#### 檢查清單

在整合 plan 結果到非 plan 類文件時，確認：

- [ ] 已移除所有階段規劃描述（Phase 1、2、3 等）
- [ ] 已改為描述最終完成狀態
- [ ] 已移除「進行中」、「已完成」等階段性標記
- [ ] 已使用現在時態描述當前運作方式
- [ ] 已將「階段配置」改為「當前配置狀態」或「系統狀態」
- [ ] 已將「實施結果」改為「系統功能」或「功能說明」
- [ ] 已將「推廣策略」改為「系統運作方式」或「使用說明」

### 步驟 2: 移除已完成的 plan 文件

**完成文檔整理後，必須移除已完成的 plan 文件**：

- ❌ **不要保留**：已完成的 plan 文件應從 `docs/plans/` 目錄中刪除
- ❌ **不要標記為已完成**：不要保留已完成計劃的歷史記錄（除非有特殊需求）
- [x] **保持 plans 目錄整潔**：`docs/plans/` 目錄應只包含未完成的計劃

**例外情況**：

- 如果 plan 包含重要的歷史記錄或決策過程，可以考慮：
  - 將重要內容整理到對應的文檔中
  - 或移動到 `docs/archive/` 目錄（如果存在）

### @ARCH Tags Mapping

**重要**：在 plan 實作過程中，就應該開始 mapping @ARCH tags：

1. **開發階段開始時**

   - 創建組件/服務時立即添加 `@ARCH:START/END` 標記
   - 標記主要功能區塊

2. **實作過程中**

   - UI 元素：添加 `@ARCH: Component - UI: 元素名稱`
   - 功能邏輯：添加 `@ARCH: Component - FEAT: 功能名稱`
   - 使用者流程：添加 `@ARCH: Component - UX: 流程名稱`

3. **完成時**
   - 確保所有 @ARCH tags 都已正確標記
   - 在 `docs/features/` 文檔中記錄 @ARCH tags 的位置和 hash
   - 更新 `ARCHITECTURE.md` 中的對應資訊

### 範例流程

````markdown
## Plan: 新增字庫管理功能

**行動**：

- [x] 創建 WordLibrary 組件
- [x] 實作字庫列表顯示
- [x] 實作新增單字功能
- [ ] 實作批次匯入功能

## 實作過程中的 @ARCH Tags

```typescript
// @ARCH:START WordLibrary - UI: 字庫管理介面
export default function WordLibrary() {
  // @ARCH: WordLibrary - UI: 字庫列表
  const [words, setWords] = useState([]);

  // @ARCH: WordLibrary - FEAT: 載入字庫
  useEffect(() => {
    loadWords();
  }, []);

  // @ARCH: WordLibrary - UX: 新增單字流程
  const handleAddWord = () => {
    // ...
  };
}
// @ARCH:END WordLibrary - UI: 字庫管理介面
```
````

## 完成後

✅ 已整併到 `docs/features/word-library.md`
✅ @ARCH tags 已記錄在功能文檔中
✅ 已移除 plan 文件

### 工作流程

```
Plan 開始
↓
實作過程中添加 @ARCH tags
↓
完成功能開發
↓
整理結果到對應文檔
  ├── docs/features/*.md (功能相關)
  ├── docs/annotations/*.md (註解系統相關)
  ├── docs/maintenance/*.md (維護系統相關)
  └── ARCHITECTURE.md (架構相關)
↓
執行檢查工具驗證
↓
移除已完成的 plan 文件
↓
提交代碼
```

### 範例：處理已完成的 plan

假設 `docs/plans/annotation-improvements.md` 已完成：

1. **整理結果**：

   - 更新 `docs/annotations/system.md`（添加自動生成工具說明）
   - 更新 `docs/annotations/examples.md`（添加使用範例）
   - 更新 `package.json`（添加 `arch:generate` script）

2. **移除 plan**：

   ```bash
   rm docs/plans/annotation-improvements.md
   ```

3. **驗證**：
   ```bash
   npm run workflow:check
   npm run check-architecture
   ```

## 相關文檔

- [文檔目錄](../README.md) - 返回文檔目錄
- [註解系統](../annotations/system.md) - 註解系統說明
- [維護系統](../maintenance/maintenance.md) - 維護系統說明

```

```
